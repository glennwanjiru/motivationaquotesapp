name: motivationaquotesapp
description: A new Flutter project.

publish_to: 'none' #Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
 sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter: 
    sdk: flutter
  hive_generator: ^1.1.1
  build_runner: ^2.1.11

  hive: ^2.2.3
  hive_flutter: ^1.1.0

  english_words: ^4.0.0
  provider: ^6.0.0

  admob_flutter: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true  



